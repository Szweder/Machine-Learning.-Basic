Домашнее задание №6: Классы и наследование
📌 Цель
Разработка иерархии классов с использованием базового класса Vehicle и его наследников (Car, Plane), а также организация обработки ошибок с помощью пользовательских исключений.

🧩 Структура проекта
csharp
Копировать
Редактировать
hw-6/
├── base.py             # Базовый класс Vehicle
├── car.py              # Класс Car, наследник Vehicle
├── plane.py            # Класс Plane, наследник Vehicle
├── engine.py           # Дата-класс Engine
├── exceptions.py       # Пользовательские исключения
└── README.md           # Описание проекта
📦 Описание компонентов
base.py
Содержит базовый класс Vehicle со следующими атрибутами и методами:

weight, started, fuel, fuel_consumption

start() — запускает двигатель, если есть топливо

move(distance) — перемещает транспорт, расходуя топливо

car.py
Класс Car — наследник Vehicle:

Атрибут engine

Метод set_engine(engine: Engine) — установка двигателя

plane.py
Класс Plane — наследник Vehicle:

Атрибуты cargo, max_cargo

Методы:

load_cargo(amount) — загрузка груза

remove_all_cargo() — выгрузка всего груза

engine.py
Дата-класс Engine с полями:

volume: float

pistons: int

exceptions.py
Содержит пользовательские исключения:

LowFuelError

NotEnoughFuel

CargoOverload

🛠 Пример использования
python
Копировать
Редактировать
from car import Car
from engine import Engine

car = Car(weight=1200, fuel=50, fuel_consumption=5)
engine = Engine(volume=1.6, pistons=4)
car.set_engine(engine)
car.start()
car.move(5)  # Расходует 25 топлива
✅ Критерии выполнения
Использованы магические методы и наследование

Классы проектированы с возможностью масштабирования

Реализованы пользовательские исключения

Оформление и структура соответствуют требованиям задания
